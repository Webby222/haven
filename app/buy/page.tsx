import ListingPage from "../components/ListingPage";

export default function BuyPage() {
  return <ListingPage purpose="buy" title="Find a property to call your own." description="Explore homes, apartments and properties available for sale across Nigeria." />;
}