import Link from "next/link";
import Footer from "../../components/Footer";
import Header from "../../components/Header";
import PropertyGrid from "../../components/PropertyGrid";
import Reveal from "../../components/Reveal";
import { properties } from "../../../lib/properties";

export default async function PropertyDetailsPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const property = properties.find((item) => item.id === id);

  if (!property) {
    return <div className="min-h-screen bg-[#fbfaf8] text-[#162b3d]"><Header /><main className="mx-auto max-w-3xl px-6 py-28 text-center sm:px-8"><p className="text-xs font-bold uppercase tracking-[0.22em] text-[#b56d45]">Property unavailable</p><h1 className="mt-5 font-serif text-5xl">We couldn&apos;t find that property.</h1><p className="mx-auto mt-5 max-w-md text-sm leading-7 text-[#667582]">The property may have moved, or the link may be incomplete. Return to the catalogue to continue exploring.</p><Link href="/properties" className="mt-8 inline-flex min-h-12 items-center bg-[#162b3d] px-7 text-sm font-bold text-white transition hover:bg-[#b56d45]">Back to properties <span className="ml-3" aria-hidden="true">→</span></Link></main><Footer /></div>;
  }

  const related = properties.filter((item) => item.id !== property.id).sort((a, b) => {
    const score = (candidate: typeof property) => Number(candidate.city === property.city) + Number(candidate.purpose === property.purpose) + Number(candidate.propertyType === property.propertyType);
    return score(b) - score(a);
  }).slice(0, 3);

  return <div className="min-h-screen bg-[#fbfaf8] text-[#162b3d]"><Header /><main className="mx-auto max-w-7xl px-6 py-12 sm:px-8 lg:px-12 lg:py-20"><Link href="/properties" className="inline-flex items-center text-sm font-bold text-[#b56d45] transition hover:text-[#162b3d]">← <span className="ml-3">Back to properties</span></Link><div className="mt-8 grid gap-10 lg:grid-cols-[1.2fr_0.8fr] lg:items-start"><Reveal className="overflow-hidden bg-[#e8ecec]"><img src={property.image} alt={property.imageAlt} className="haven-image-reveal aspect-[4/3] h-full w-full object-cover" /></Reveal><Reveal delay={120} className="lg:pt-5"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#b56d45]">For {property.purpose === "sale" ? "sale" : "rent"} · {property.propertyType}</p><h1 className="mt-5 font-serif text-5xl leading-[0.96] tracking-[-0.03em] sm:text-6xl">{property.title}</h1><p className="mt-5 text-base text-[#667582]">{property.location}, {property.city}</p><p className="mt-8 font-serif text-3xl text-[#b56d45]">{property.price}</p><dl className="mt-8 grid grid-cols-2 gap-y-5 border-y border-[#dfe3e4] py-6 text-sm"><div><dt className="text-[#8b99a3]">Bedrooms</dt><dd className="mt-1 font-bold">{property.bedrooms}</dd></div><div><dt className="text-[#8b99a3]">Bathrooms</dt><dd className="mt-1 font-bold">{property.bathrooms}</dd></div><div><dt className="text-[#8b99a3]">Parking</dt><dd className="mt-1 font-bold">{property.parking}</dd></div><div><dt className="text-[#8b99a3]">Property type</dt><dd className="mt-1 font-bold">{property.propertyType}</dd></div></dl><p className="mt-8 text-sm leading-7 text-[#667582]">{property.description}</p><Link href={`/contact?property=${encodeURIComponent(property.title)}`} className="mt-8 inline-flex min-h-12 items-center bg-[#162b3d] px-7 text-sm font-bold text-white transition hover:bg-[#b56d45]">Enquire about this property <span className="ml-3" aria-hidden="true">→</span></Link></Reveal></div><section className="mt-16 grid gap-10 border-t border-[#dfe3e4] pt-10 lg:grid-cols-[0.8fr_1.2fr]" aria-labelledby="features-heading"><Reveal><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#b56d45]">Property details</p><h2 id="features-heading" className="mt-3 font-serif text-4xl">Made for the way you live.</h2></Reveal><Reveal delay={100}><div className="grid grid-cols-2 gap-3 sm:grid-cols-3">{property.features.map((feature) => <div key={feature} className="border border-[#dfe3e4] bg-white px-4 py-5 text-sm font-semibold">{feature}</div>)}{property.furnished && <div className="border border-[#dfe3e4] bg-white px-4 py-5 text-sm font-semibold">Furnished</div>}{property.serviced && <div className="border border-[#dfe3e4] bg-white px-4 py-5 text-sm font-semibold">Serviced</div>}</div></Reveal></section><section className="mt-24" aria-labelledby="related-heading"><div className="flex items-end justify-between border-b border-[#dfe3e4] pb-6"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#b56d45]">Keep exploring</p><h2 id="related-heading" className="mt-3 font-serif text-4xl">You might also like</h2></div><Link href="/properties" className="hidden text-sm font-bold text-[#b56d45] sm:block">View all properties →</Link></div><div className="mt-8"><PropertyGrid properties={related} /></div></section></main><Footer /></div>;
}
